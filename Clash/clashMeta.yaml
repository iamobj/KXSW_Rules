# HTTP(S) and SOCKS4(A)/SOCKS5 server on the same port
mixed-port: 7899
redir-port: 7900
tproxy-port: 7901

# authentication of local SOCKS5/HTTP(S) server
# authentication:
#  - "user1:pass1"
#  - "user2:pass2"

# Set to true to allow connections to the local-end server from
# other LAN IP addresses
allow-lan: true # 允许局域网的连接（共享代理）

# This is only applicable when `allow-lan` is `true`
# '*': bind all IP addresses
# 192.168.122.11: bind a single IPv4 address
# "[aaaa::a8aa:ff:fe09:57d8]": bind a single IPv6 address
bind-address: '*'

# Clash router working mode
# rule: rule-based packet routing
# global: all packets will be forwarded to a single endpoint
# direct: directly forward the packets to the Internet
mode: rule

# Clash by default prints logs to STDOUT
# info / warning / error / debug / silent
log-level: info

# When set to false, resolver won't translate hostnames to IPv6 addresses
ipv6: false

# RESTful web API listening address
external-controller: 127.0.0.1:9399

# A relative path to the configuration directory or an absolute path to a
# directory in which you put some static web resource. Clash core will then
# serve it at `${API}/ui`.
# external-ui: folder

# Secret for the RESTful API (optional)
# Authenticate by spedifying HTTP header `Authorization: Bearer ${secret}`
# ALWAYS set a secret if RESTful API is listening on 0.0.0.0
# secret: ""

# Outbound interface name
# interface-name: en0

# Static hosts for DNS server and connection establishment (like /etc/hosts)
#
# Wildcard hostnames are supported (e.g. *.clash.dev, *.foo.*.example.com)
# Non-wildcard domain names have a higher priority than wildcard domain names
# e.g. foo.example.com > *.example.com > .example.com
# P.S. +.foo.com equals to .foo.com and foo.com
# 本地 dns 解析，和系统 hosts 一样，优先级高于 dns 服务器，use-hosts 为 true 时生效
# hosts:
# '*.clash.dev': 127.0.0.1
# '.dev': 127.0.0.1
# 'alpha.clash.dev': '::1'

profile:
  # Store the `select` results in $HOME/.config/clash/.cache
  # set false If you don't want this behavior
  # when two different configurations have groups with the same name, the selected values are shared
  store-selected: true

  # persistence fakeip
  # store-fake-ip: false

# 开启统一延迟时，会计算 RTT，以消除连接握手等带来的不同类型节点的延迟差异
unified-delay: true

# TCP Keep Alive 包的间隔，单位为秒，较大数值可以避免异常的资源消耗
keep-alive-interval: 1800

# TCP 并发请求所有 IP 地址，然后选择延迟最低的一个进行连接
tcp-concurrent: true

# 全局客户端指纹，这里设置随机生成一个现代浏览器指纹
global-client-fingerprint: random

# 域名嗅探
sniffer:
  enable: true
  force-dns-mapping: true
  parse-pure-ip: true
  skip-domain:
    - Mijia Cloud
  sniff:
    HTTP:
      ports: [80, 8080-8880]
    TLS:
      ports: [443, 8443]
    QUIC:
      ports: [443, 8443]

tun:
  enable: true
  stack: mixed
  dns-hijack:
    - any:53
  auto-route: true
  auto-detect-interface: true

# 自动更新 GeoIP
geo-auto-update: true

# 自动更新 GeoIP 的间隔，单位为小时
geo-update-interval: 24
geox-url:
  geoip: 'https://testingcf.jsdelivr.net/gh/MetaCubeX/meta-rules-dat@release/geoip.dat'
  geosite: 'https://testingcf.jsdelivr.net/gh/MetaCubeX/meta-rules-dat@release/geosite.dat'
  mmdb: 'https://testingcf.jsdelivr.net/gh/MetaCubeX/meta-rules-dat@release/country.mmdb'
  asn: 'https://github.com/xishang0128/geoip/releases/download/latest/GeoLite2-ASN.mmdb'

# 自定义外部资源下载时使用的的 UA
global-ua: clash.meta

# DNS server settings
# This section is optional. When not present, the DNS server will be disabled.
dns:
  enable: true
  listen: 0.0.0.0:1053
  enhanced-mode: redir-host # redir-host fake-ip
  # fake-ip-range: 198.18.0.1/16
  # fake-ip-filter:
  #   - '+.*'

  # These nameservers are used to resolve the DNS nameserver hostnames below.
  # Specify IP addresses only
  default-nameserver: # 解析 nameserver 里非 IP 的 dns 用的 dns 服务器，只支持纯 IP
    - 180.76.76.76
    - 180.184.1.1
    - 180.184.2.2
    - 114.114.114.114
    - 119.29.29.29

  nameserver-policy:
    geosite:cn,apple,private:
      - 114.114.114.114
      - 180.76.76.76
      - 180.184.1.1
      - 180.184.2.2
  # Supports UDP, TCP, DoT, DoH. You can specify the port to connect to.
  # All DNS questions are sent directly to the nameserver, without proxies
  # involved. Clash answers the DNS question with the first result gathered.
  nameserver: # 这里最好使用公共 dns 服务器，不使用运营商的 dns 服务器，防止 dns 污染
    - https://8.8.8.8/dns-query # 使用 DoH 加密 DNS 查询，这里使用 ip 地址，不使用域名，可以避免使用 default-nameserver 解析域名这个动作
    - https://1.12.12.12/dns-query
    - https://hk-hkg.doh.sb/dns-query
    - https://doh.pub/dns-query
    - https://120.53.53.53/dns-query
    - https://doh.360.cn/dns-query
    - https://223.6.6.6/dns-query
    - https://1.1.1.1/dns-query
    - tls://8.8.4.4
    - tls://208.67.222.222

proxies:

# 服务器节点订阅
proxy-providers:

proxy-groups:
  - name: 'PROXY'
    type: select
    use:
    proxies:

  - name: 'MATCH'
    type: select
    proxies:
      - PROXY
      - DIRECT

# 使用jsdelivr加速访问github资源，但需要注意缓存，jsdelivr 缓存会在24小时内刷新，并不是实时刷新，可以把 https://cdn.jsdelivr.net/ 换成 https://purge.jsdelivr.net/ 访问可达到手动刷新缓存

# 关于 Rule Provider 请查阅：https://lancellc.gitbook.io/clash/clash-config-file/rule-provider

rule-providers:
  # name: # Provider 名称
  #   type: http # http 或 file
  #   behavior: classical # 或 ipcidr、domain
  #   path: # 文件路径
  #   url: # 只有当类型为 HTTP 时才可用，您不需要在本地空间中创建新文件。
  #   interval: # 自动更新间隔，仅在类型为 HTTP 时可用

  # 需要直连的
  Unbreak:
    type: http
    behavior: classical
    path: ./RuleSet/Unbreak.yaml
    url: https://raw-github.rhc.im/iamobj/KXSW_Rules/m/Clash/RuleSet/Unbreak.yaml
    interval: 86400

  # 国外流媒体
  Streaming:
    type: http
    behavior: classical
    path: ./RuleSet/Streaming.yaml
    url: https://raw-github.rhc.im/iamobj/KXSW_Rules/m/Clash/RuleSet/Streaming.yaml
    interval: 86400

  # 需要代理的
  Global:
    type: http
    behavior: classical
    path: ./RuleSet/Global.yaml
    url: https://raw-github.rhc.im/iamobj/KXSW_Rules/m/Clash/RuleSet/Global.yaml
    interval: 86400

  ChinaIP:
    type: http
    behavior: ipcidr
    path: ./RuleSet/ChinaIP.yaml
    url: https://raw-github.rhc.im/iamobj/KXSW_Rules/m/Clash/RuleSet/ChinaIP.yaml
    interval: 86400

  China:
    type: http
    behavior: classical
    path: ./RuleSet/China.yaml
    url: https://raw-github.rhc.im/iamobj/KXSW_Rules/m/Clash/RuleSet/China.yaml
    interval: 86400

  # 广告
  Ad:
    type: http
    behavior: classical
    path: ./RuleSet/Ad.yaml
    url: https://raw-github.rhc.im/iamobj/KXSW_Rules/m/Clash/RuleSet/Ad.yaml
    interval: 86400

# 规则
rules:
  # Unbreak
  - RULE-SET,Unbreak,DIRECT

  # Global Area Network

  # (Streaming Media)
  - RULE-SET,Streaming,PROXY

  # (DNS Cache Pollution) / (IP Blackhole) / (Region-Restricted Access Denied) / (Network Jitter)
  - RULE-SET,Global,PROXY

  # China Area Network
  - RULE-SET,China,DIRECT
  # - RULE-SET,ChinaIP,DIRECT

  - RULE-SET,Ad,REJECT

  # Local Area Network
  - IP-CIDR,192.168.0.0/16,DIRECT,no-resolve
  - IP-CIDR,10.0.0.0/8,DIRECT,no-resolve
  - IP-CIDR,172.16.0.0/12,DIRECT,no-resolve
  - IP-CIDR,127.0.0.0/8,DIRECT,no-resolve
  - IP-CIDR,100.64.0.0/10,DIRECT,no-resolve
  - IP-CIDR,224.0.0.0/4,DIRECT,no-resolve
  - IP-CIDR,fe80::/10,DIRECT,no-resolve

  # Tencent
  - IP-CIDR,119.28.28.28/32,DIRECT,no-resolve
  - IP-CIDR,182.254.116.0/24,DIRECT,no-resolve
  # GeoIP China   好多国外服务的 ip 也会被归属到 CN 导致直连，注释掉
  # - GEOIP,CN,DIRECT
  - MATCH,MATCH
